// Eksempel på Variabler
let antalKunder = 150;
const produktPris = 99.99;
let kundeNavn = "Jens Hansen";

// Eksempel på Datatyper
let alder = 30;                        // Number
let navn = "Anna";                     // String
let erAktiv = true;                    // Boolean
let priser = [10.50, 20.75, 30.00];    // Array of numbers

// Eksempel på Løkker
for (let i = 0; i < 5; i++) {
    console.log(`Iteration ${i + 1}`);
}

// Eksempel på Betingelser
let temperatur = 18;
if (temperatur < 20) {
    console.log("Det er for koldt.");
} else {
    console.log("Temperaturen er behagelig.");
}
